services:
  backend:
    build:
      context: ./backend
    container_name: backend
    volumes:
      - ./backend:/code
    ports:
      - "8000:8000"

  frontend:
    build:
      context: ./frontend
    container_name: frontend
    ports:
      - "3000:80"
    depends_on:
      - backend
